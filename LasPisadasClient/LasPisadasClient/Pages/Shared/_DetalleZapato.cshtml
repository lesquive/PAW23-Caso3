@model LasPisadasClient.Pages.IndexModel.Zapato

<div class="card mb-3">
    <div class="card-body">
        <h5 class="card-title">@Model.Marca - @Model.Modelo</h5>
        <p class="card-text">@Model.DescripcionDetallada</p>
        <p class="card-text">Precio: $@Model.Precio.ToString("0.##")</p>
        <a href="#" class="btn btn-primary">Comprar Ahora</a>
    </div>
</div>
